# GamePanel Lite 领域语言

以下术语区分用户拥有的游戏服务器与各区域实际运行的资源。

## Language

**User（用户）**：平台上的个人身份，可以加入多个组织。用户不是计算节点，也不等同于计费账户。

**Organization（组织／租户）**：成员权限、资源归属和商业权益的单位；个人空间也是一个组织。
_Avoid_: 与 Organization 并列的另一套 Tenant 实体

**GameServer（逻辑实例）**：用户拥有的稳定游戏服务器身份，包含全局配置与期望状态，跨区域迁移不改变其身份。
_Avoid_: 容器、节点实例、区域部署

**ServerRevision（配置版本）**：逻辑实例某一版本的完整用户配置、规格及资产版本引用。

**Placement（部署归属）**：逻辑实例的目标 Region 及当前有效部署归属。
_Avoid_: 节点、容器

**Deployment（区域部署）**：逻辑实例在某一区域的一次部署，拥有节点分配、执行任务和运行结果。

**RuntimeInstance（运行实例）**：区域部署在节点上的具体游戏进程与容器；替换运行实例不改变逻辑实例身份。

**Region（区域）**：一个地理范围内独立管理部署、资源、监控和恢复的区域体系。

**Node（计算节点）**：归属于唯一 Region 的宿主机，可承载多个运行实例。

**SchedulingRequirement（调度要求）**：指定区域、规格、授权节点池及可选严格指定节点的用户或运维要求。
_Avoid_: 实际节点分配

**Allocation（资源分配）**：区域为部署实际分配的节点、计算容量、端口和存储资源。

**Operation（操作）**：创建、迁移、恢复或删除等具有稳定身份、进度和结果的长任务。

**Entitlement（权益）**：组织根据购买和商业规则被允许使用的资源范围与期限。

**Asset（逻辑资产）**：具有稳定身份、组织归属和版本的世界存档、备份或模组。

**PublishedAssetVersion（已发布资产版本）**：完成内容校验后登记的不可变资产版本，固定内容摘要及大小；发布不表示每个 Region 都已具有可用副本，也不授予运行写入权。

**AssetReplica（资产副本）**：逻辑资产某个版本在具体区域或存储位置的物理副本；运行中的世界存档具有唯一有效写入者。

**StorageID（存储标识）**：Region 内受信存储资源的稳定身份；不是宿主机路径、下载 URL 或访问凭证。
